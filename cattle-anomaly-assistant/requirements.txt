# M0 (shared schemas + mock generator) + M2a (KB + Stage 2 pipeline, fake LLM
# client only). Stage 1 will add numpy/scikit-learn/torch when its code is
# ported in; a real LLM SDK gets added once a provider is chosen (PRD Open
# Question 2); ragas lands with M2b. See LLM_ASSISTANT_STATUS.md for status.
pydantic>=2.5
pytest>=8.0
pyyaml>=6.0
