# Was stale: listed only pydantic/pytest/pyyaml and a comment claiming Stage 1
# would need torch, even after the WASP behavior classifier (numpy/
# scikit-learn/xgboost, no torch anywhere in the codebase) and MmCows CUSUM
# baseline were both ported in — a plain `pip install -r requirements.txt`
# produced a broken, ML-less Stage 1. Regenerated from pyproject.toml's own
# dependencies + the `behavior` extra, which was already correct; a real LLM
# SDK still gets added once a provider is chosen (PRD Open Question 2). See
# LLM_ASSISTANT_STATUS.md for status, `pip install -e '.[behavior,dev]'` for
# the equivalent editable install.
pydantic>=2.5
pyyaml>=6.0
numpy>=1.26,<3
scikit-learn>=1.4,<2
xgboost>=2.0,<4
pytest>=8.0
