# M0 (shared schemas + mock generator) + M2a (KB + Stage 2 pipeline, fake LLM
# client only) + M2b (Ragas Layer 2 eval harness, fake judge only). Stage 1
# will add numpy/scikit-learn/torch when its code is ported in; a real LLM
# SDK gets added once a provider is chosen (PRD Open Question 2). See
# LLM_ASSISTANT_STATUS.md for status.
pydantic>=2.5
pytest>=8.0
pytest-asyncio>=1.0
pyyaml>=6.0
ragas==0.4.3
