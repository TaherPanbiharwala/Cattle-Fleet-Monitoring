# LLM Diagnostic Assistant — Shared Context, Milestones & Progress

**Last updated:** 2026-09-11
**Source PRD:** [LLM_Diagnostic_Assistant_PRD.md](LLM_Diagnostic_Assistant_PRD.md) — frozen spec. Don't edit it without flagging the change to MTZ; this file tracks status *against* it.
**Purpose of this file:** MTZ is going to run Codex, Claude Code, and Antigravity against this same repo, in no particular order, possibly in different worktrees. None of those tools share memory with each other. This file is the thing that does — paste it, read it, update it.

---

## 1. Paste this block to onboard a fresh session

> You're working in the "Intelligent Cattle Fleet Management Platform" repo — an ESP32 cattle-collar IoT project with a Python digital-twin simulator. It is **not** an empty repo. It already has: a complete Phase 1 herd simulator (411 tests as of `DECISION.md` ADR-022 — two Phase-3 test files landed after that count and aren't reflected in it), working physical-collar firmware (ADR-023/024), and a just-added, not-yet-run Phase 3 behavior-classification benchmark on the public WASP-lab/`db-cow-walking` dataset (`src/ml/`, `src/dataset_adapters/wasp_lab.py`, `PHASE3_BENCHMARK.md`).
>
> We are now building a new component specified in full in `LLM_Diagnostic_Assistant_PRD.md`: a two-stage system — **Stage 1** (classical ML: a behavior classifier plus a per-cow SPC/CUSUM baseline over `MmCows` data) producing a structured `AnomalyRecord`, and **Stage 2** (a hard-grounded RAG+LLM layer with a calibrated fallback gate) that explains what Stage 1 detected in cited, farmer-readable language. No disease diagnosis anywhere in this system — anomaly detection only.
>
> This new work replaces this repo's own **Phase 4** (personal deviation) and **Phase 5** (fever/lameness research) from `Cattle_Fleet_Management_Master_PRD.md`. **MTZ confirmed on 2026-09-10 that this is a deliberate pivot, not something to reconcile**: wherever `LLM_Diagnostic_Assistant_PRD.md` conflicts with the old Master PRD's P4/P5 design, the new PRD wins outright — see Section 2 below for exactly what that overrides, so you don't accidentally resurrect the old formula or gate.
>
> Read, in this order: this file in full → `LLM_Diagnostic_Assistant_PRD.md` in full → `AGENTS.md`'s golden rules (§3 — they still bind: no diagnosis claims, cow-grouped cross-validation only, never map Miscellaneous behavior to Restless, secrets via env only) → skim `DECISION.md`'s ADR index for anything touching what you're about to change.
>
> **Milestones M0, M2a, M2b, and M2c are all done** (2026-09-10/11): M0 is the schema contract + mock generator (`shared/schemas/`, all models inheriting a `StrictModel` base with `extra="forbid"` and `AwareDatetime` timestamps — see the 2026-09-10 `/review` entry in §8 for why). M2a is the full Stage 2 pipeline — a SQLite KB (`kb/`), an LLM client abstraction currently backed only by a deterministic `FakeLLMClient` (`llm/`, real provider deferred by MTZ's own choice, see §4 Q2), and all 5 pipeline stages wired end to end (`pipeline/`, entry point `pipeline/orchestrator.run_pipeline()`). M2b is the Layer 2 (Ragas) eval harness (`eval/`) — a mock-derived + hand-written-adversarial golden set, a fake Ragas judge mirroring the same deferred-provider policy, and a `run_eval.py` report — read `eval/_ragas_compat.py`'s docstring before touching anything that imports `ragas` directly, it explains a real upstream bug you'd otherwise rediscover the hard way. M2c is a τ-calibration prototype (PRD §10 steps 1-3 only, `calibration/`) — never mistake its report for a real calibrated tau, and never resurrect PRD step 4 ("Stage 1's own accuracy") on the mock golden set, it reads as ~100% by construction and is explicitly not computed here. All four live in a new self-contained subproject, [`cattle-anomaly-assistant/`](cattle-anomaly-assistant/), with its own `pyproject.toml`/`requirements.txt`/`.venv`/`tests/`, and none import from `../src/` — code reused from the existing WASP-lab work would be copied in, not cross-imported (see `cattle-anomaly-assistant/README.md`). **2026-09-11: a real LLM provider (OpenRouter, `minimax/minimax-m3`) is now wired for the main pipeline, and the Stage 1 branch has been merged into this one.** Stage 1 is real: M1b/M1c (MmCows CUSUM + injection harness) are done and validated against real data; M1a's own from-scratch classifier fails its real benchmark's eligibility gate, so the accepted M1a deliverable is a pre-trained external reference model instead (`stage1_anomaly_detection/behavior_classifier/reference_model/`), consumed via a cross-dataset bridge that never claims same-cow provenance — that bridge, not same-cow fusion, is now the accepted Integration Point mechanism, since this project will never have real same-deployment cow data. See §5's Stage 1 track and the Integration Point section for the full detail, and §8's 2026-09-11 merge entry for exactly what that merge involved. Read what's there before writing more of it; requires Python 3.11-3.13 (`pip install -e '.[behavior,dev]'`, not plain 3.14, for the Stage 1 ML deps) — 177/177 tests pass (`cd cattle-anomaly-assistant && .venv/bin/python -m pytest tests/ -v`).
>
> Do not silently resolve anything marked **[OPEN QUESTION]** in the PRD or in §4 below — surface it instead. When you finish a chunk of work, tick its checkbox in §5 and append a dated entry to the Progress Log in §8 — don't leave it implicit in a commit message, since the other two tools can only see this file, not your shell history.

---

## 2. How this fits the existing platform (read before assuming anything is greenfield)

The PRD you were handed says "nothing has been implemented yet... greenfield build." That's true for Stage 2 (the RAG/LLM half). It is **not** true project-wide: this repo already has a mature 5-phase platform (`AGENTS.md`, `DECISION.md`, `Cattle_Fleet_Management_Master_PRD.md`), and the new PRD's Stage 1 overlaps two of those phases directly, not by coincidence — same datasets, adjacent authors, same student project.

| Existing platform phase | New PRD equivalent | Relationship |
|---|---|---|
| **P3** — WASP-lab/`db-cow-walking` behavior classification (scaffolded 2026-09-04: `src/ml/`, `src/dataset_adapters/wasp_lab.py`, `PHASE3_BENCHMARK.md`) | Stage 1, task **1a** | Same dataset, same task. Reuse this code — do not rebuild it from scratch. |
| **P4** — Personalized per-cow deviation (`Cattle_Fleet_Management_Master_PRD.md`: median/MAD robust z-score, baseline data from the simulator's "first complete simulated day" or 7 healthy physical-collar days) | Stage 1, task **1b** | **Deliberately different, not an extension.** The new PRD uses SPC/CUSUM (not median/MAD) and the public `MmCows` dataset (not simulator/collar data) for baselining. Treat P4's original formula as superseded — confirm that's actually the intent (see conflict below). |
| **P5** — Fever/lameness risk research (label-gated disease prediction) | Stage 2 | **Dropped, not extended.** The new PRD never outputs a disease label anywhere (PRD §4/§5) — a deliberate scope narrowing, since neither dataset has illness ground truth to train or validate a diagnosis against. |

Two concrete conflicts, both **resolved 2026-09-10 in favor of the new PRD** — recorded here so nobody re-derives the old numbers by habit:

- **Metric bar mismatch.** `PHASE3_BENCHMARK.md`'s own release gate is cow-grouped macro F1 ≥ 0.85 (Walking / Other-Unknown recall ≥ 0.75). The new PRD (§2) targets beating the *published* SVM baseline (96.29% accuracy / 0.9625 macro-F1) under leave-one-cow-out CV. **Updated 2026-09-11:** the published 0.9625 came from a random split and is scientifically non-comparable to LOCO, so it is a reference only. M1a's only artifact-eligibility evidence is nested LOCO; its provisional public-benchmark gate is mean outer-fold macro F1 ≥0.85 plus pooled Walking/Miscellaneous recall ≥0.75. Passing remains neither field nor clinical validation.
- **Formula replacement.** `Cattle_Fleet_Management_Master_PRD.md`'s P4 section spells out a specific formula: `z_j = (x_j - median_j) / max(1.4826·MAD_j, ε)`, `D = median of the 3 largest |z|`, `risk = round(100·(1 - exp(-max(D-1,0)/3)))`. **Resolved: this formula is dropped, full stop.** M1b implements SPC/CUSUM per the new PRD's §6/§10, with no obligation to also produce a median/MAD score anywhere.

## 3. Non-negotiables that still apply (from `AGENTS.md`, unchanged by the new PRD)

- Never map Miscellaneous / low-confidence behavior to Restless (`AGENTS.md` golden rule 6; restated in `PHASE3_BENCHMARK.md`).
- No clinical/veterinary diagnosis claims anywhere (golden rule 7) — the new PRD's §5 non-goals restate this even more strictly for Stage 2 specifically.
- Cow-grouped / leave-one-cow-out evaluation only — never split windows from the same cow across train/test (`DECISION.md` ADR-015).
- Secrets via environment/`.env` only, never hardcoded or logged (golden rule 5; PRD §12).
- **New in this PRD:** raw IMU samples or raw temperature time series must never reach the LLM in Stage 2 (PRD §5 non-goal) — Stage 1 always sits between the sensors and the LLM. This is stricter than anything P1–P3 required, since none of them touch an LLM at all.

## 4. Open questions blocking design decisions

From the PRD itself (§16):

1. Confirm the KB's target shift categories (PRD §9) or adjust. **Seeded 2026-09-10** with the PRD's own 5 proposed categories as the default answer, in an editable YAML file ([`cattle-anomaly-assistant/stage2_rag_assistant/kb/seed_data/shift_categories.yaml`](cattle-anomaly-assistant/stage2_rag_assistant/kb/seed_data/shift_categories.yaml)) specifically so this stays cheap to adjust — still genuinely open, not resolved, just unblocked.
2. ~~LLM provider/model choice for Stage 2~~ (PRD §12's pricing table is a starting point, not a decision). **Deferred 2026-09-10, resolved 2026-09-11 for the main pipeline: OpenRouter, `minimax/minimax-m3`.** [`llm/providers/openrouter_provider.py`](cattle-anomaly-assistant/stage2_rag_assistant/llm/providers/openrouter_provider.py) — stdlib `urllib`, no SDK dependency, `OPENROUTER_API_KEY` read from the environment only. `default.yaml` stays on `fake` (tests/bare CLI runs still need zero API keys); real runs pass `--pipeline-config stage2_rag_assistant/config/openrouter.yaml` explicitly. `build_llm_client()` still raises `NotImplementedError` for `anthropic`/`openai`/`google` directly (not via OpenRouter) — unchanged, still deferred, nobody's asked for those. **Still open:** the Ragas eval judge (`eval/ragas_llm.py`) is a separate, unrelated decision — wiring it to a real provider means a real paid call per metric per golden case on every `run_eval` invocation, not a one-line change to make lightly.
3. Verify the MultiGraph-vet paper (PRD §3) directly — not blocking.
4. Solo build or split across teammates for the two tracks.
5. ~~Which coding tool~~ — **answered**: all three (Codex, Claude Code, Antigravity) will be used against this same repo. See §7 for the coordination risk that creates.

Found while reconciling the PRD against the existing repo (not in the original document) — **asked MTZ directly on 2026-09-10:**

6. ~~**Repo layout.**~~ — **Resolved 2026-09-10: new top-level folder in this same repo**, per PRD §13's own tree. Lives at [`cattle-anomaly-assistant/`](cattle-anomaly-assistant/), self-contained (own `pyproject.toml`/`requirements.txt`/`tests/`), not importing from `../src/`. Reused WASP-lab code gets copied in as a starting point, not cross-imported — see the M1a note below.
7. ~~**What happens to the old Master PRD / `DECISION.md` / P1-P2 code.**~~ — **Resolved 2026-09-10: leave everything as-is.** `Cattle_Fleet_Management_Master_PRD.md`, `DECISION.md`, `herd_simulator/`, and `collar_gateway/` are untouched, no annotation, no archiving. They simply stop being the active roadmap; this file is what says so.

~~Which F1 gate governs M1a~~ — resolved, folded into §2 above.

## 5. Milestones & progress

Legend: ⬜ not started · 🟨 in progress / partially built · ✅ done

### M0 — Shared contract & mock generator (Sprint 0, joint, blocks everything else)
**Status: ✅ Done (2026-09-10)**
- [x] Decide repo layout (Open Question 6) — `cattle-anomaly-assistant/`, new top-level folder, this repo
- [x] `AnomalyRecord`, `AnomalyExplanationQuery`, `AnomalyExplanationResponse`, `GoldenCase` as pydantic models (PRD §6.1, 6.3–6.5) — [`cattle-anomaly-assistant/shared/schemas/`](cattle-anomaly-assistant/shared/schemas/)
- [x] Add `pydantic` to `pyproject.toml` / requirements — [`cattle-anomaly-assistant/pyproject.toml`](cattle-anomaly-assistant/pyproject.toml), [`requirements.txt`](cattle-anomaly-assistant/requirements.txt)
- [x] Mock `AnomalyRecord` generator (PRD §6.2) — [`cattle-anomaly-assistant/stage2_rag_assistant/mock/generate_mock_records.py`](cattle-anomaly-assistant/stage2_rag_assistant/mock/generate_mock_records.py); seeded, so `generate_mock_batch(n, seed=...)` is byte-identical across runs (not required by the PRD, added because it's nearly free and matches this repo's own bias toward deterministic fixtures)
- [x] Verify: [`tests/test_schemas_and_mock.py`](cattle-anomaly-assistant/tests/test_schemas_and_mock.py) — 8/8 passing (5 original + 3 added during review below). Covers schema-valid + JSON-round-trip mock output, the minority-anomaly distribution, reproducibility under a fixed seed, malformed/unknown-field/naive-timestamp/bad-distribution rejection, and `GoldenCase`/`AnomalyExplanationQuery`/`AnomalyExplanationResponse` composing correctly.
- [x] **Hardened via `/review` (2026-09-10)**, since MTZ flagged M0 as the thing everything else builds on: all models now inherit a shared [`_base.StrictModel`](cattle-anomaly-assistant/shared/schemas/_base.py) (`extra="forbid"` — pydantic's default silently drops unrecognized fields instead of erroring, verified empirically before fixing); all 4 timestamp fields switched from `datetime` to pydantic's `AwareDatetime` (a naive timestamp from one stage and an aware one from another would raise `TypeError` the first time anything compares or sorts them — worth closing now, not after three pieces of code depend on it); `BehaviorStateDistribution24h` gained a model validator rejecting a distribution whose four proportions don't sum to ~1.0 (each was independently bounded [0,1] before, so `{1.0,1.0,1.0,1.0}` used to pass). All three are stricter-only changes — verified against every existing construction call before applying, so nothing that used to validate stopped validating.

### Stage 1 track

**M1a — Behavior classifier (`db-cow-walking`)**
**Status: ✅ Resolved (2026-09-11) — accepted output is an external reference XGBoost model, not this repo's own trained classifier.** The full nested-LOCO benchmark *was* run against the real 441-event/10-cow/10,240-window WASP dataset (`~/Desktop/wasp-behavior-benchmark-001/benchmark_report.json`) and produced a real, disappointing result: `primary_mean_outer_fold_macro_f1 = 0.6326` against the code's own required `>= 0.85`, and `miscellaneous behaviors` recall `0.148` against the required `>= 0.75` — `artifact_eligibility.passed: false`. Because the gate correctly failed, `benchmark.py` never produced a qualifying artifact, and per the code's own design (see `benchmark.py:210`) none should exist from that path. MTZ's decision, given the project will never have its own labeled collar-cow data to train and properly LOCO-validate a same-cow model: **use a pre-trained, externally-sourced reference model instead, permanently, not as a stopgap.** That model (`cow_behavior_xgboost_reference.json` + its manifest/metrics, 98.8% accuracy but on a *stratified random* split, not grouped by cow — an optimistic, not real-world, number; `scope: "reference_only"`, `not_valid_for_collar_or_clinical_use: true` per its own manifest) is now checked into [`stage1_anomaly_detection/behavior_classifier/reference_model/`](cattle-anomaly-assistant/stage1_anomaly_detection/behavior_classifier/reference_model/) (see that directory's own README for the full provenance/caveats) and consumed exclusively through `historical_demo.py`'s independent 25-channel BNO055+MPU9250 code path — never through `wasp.py`/`features.py`/`benchmark.py`, which use this project's own narrower 6-channel MPU9250-only WASP adapter and remain useful only as a demonstrated-failed real-data benchmark, kept for the record.
- [x] Self-contained six-axis WASP adapter and 112-feature extraction in `cattle-anomaly-assistant/stage1_anomaly_detection/behavior_classifier/`; the historical `src/` modules are untouched and never imported. (Real result: fails its own eligibility gate — see above. Kept as real, working, honestly-negative evidence, not deleted.)
- [x] CPU XGBoost nested LOCO with native JSON-only artifacts, hash manifest, uncalibrated `softprob` confidence, public-benchmark gate, and `.pkl` rejection/provenance-only hashing.
- [x] Actual local public-data preflight: 441 events / 10 cows / 10,240 valid derived windows; full benchmark run and its real (failing) result now recorded above.
- [x] Derived behavior prediction and daily context shape (`behavior_state`, uncalibrated confidence, four-state daily distribution) matching the shared `AnomalyRecord` contract — verified 2026-09-11 by validating a real `historical-demo-records` CLI run's output (11 records) through the actual frozen `AnomalyRecord.model_validate()`.
- [x] Accepted deliverable wired and verified end-to-end: `reference_model/` checked in, `historical-demo-records` CLI run against real WASP + real MmCows CUSUM data, output validates cleanly.

**M1b — MmCows ingestion + per-cow SPC/CUSUM baseline**
**Status: ✅ Implemented, fixture-verified, and real-MmCows smoke-tested (2026-09-11).** See [`STAGE1_ANOMALY_BASELINE_HANDOFF.md`](STAGE1_ANOMALY_BASELINE_HANDOFF.md) for the onboarding and [`cattle-anomaly-assistant/README.md`](cattle-anomaly-assistant/README.md) for staged-data commands.
- [x] `MmCows` adapter: `cbt`, `ankle`, `thi`, optional `immu` (do not conflate `ankle` with raw acceleration — PRD §9 correction note)
- [x] Per-cow baseline (μ, σ) from first seven consecutive valid local daily windows
- [x] Conservative two-sided CUSUM (`k=0.5σ`, `h=5σ`), per-signal reset after gaps, derived-only atomic outputs
- [x] Explicitly confirmed in code/docs: this replaces, not supplements, the Master PRD's P4 median/MAD formula — see §2 conflict above
- [x] User-run smoke test against a staged real MmCows subset; report coverage/mechanical indicators only, not health-label claims

**M1c — Synthetic injection harness**
**Status: ✅ Implemented, fixture-verified, and real-MmCows injection-smoke-tested (2026-09-11).**
- [x] Injection harness over derived monitoring-day features (not raw sensor rows/baseline): deterministic CBT, lying-time, activity, combined, invalid, and clean-control scenarios — reusable by the §11 golden-set build
- [x] Kept distinct from `herd_simulator`'s existing scenario/fault-injection engine (`config/scenarios/*.json`, `scenario_runner.py`), which generates synthetic *telemetry* for the digital twin, not golden-set anomaly labels for this eval

**M1d — `to_anomaly_record.py`**
**Status: ✅ Contract implemented and fixture-verified; real public-data fusion deliberately deferred.**
- [x] Versioned `BehaviorDailyContext` / `CusumDailyResult`, exact same-cow/day/timezone/deployment join, atomic JSONL `AnomalyRecord` publication, and Stage 2 assembler compatibility test.
- [x] Current MmCows CUSUM output adapts to explicit `mmcows_public` provenance and is rejected alongside `wasp_public`; no fabricated cross-dataset record is produced.
- [x] Historical-dataset application bridge: the active Kaggle 25-channel XGBoost model produces a derived WASP behavior summary attached to flagged MmCows records with explicit cross-dataset-demo provenance; CUSUM remains the anomaly source.
- [x] **Resolved 2026-09-11 (MTZ):** `fuse_daily_records`' real same-cow fusion path stays fail-closed permanently, not "until a future deployment" — the project will never have its own aligned same-cow runtime behavior + physiology data to feed it. The cross-dataset historical-demo bridge (`historical_demo_anomaly_records`) is the accepted, permanent Integration Point mechanism, not a placeholder — see the Integration Point section below.

### Stage 2 track (unblocked as soon as M0 lands — don't wait on Stage 1)

**M2a — KB + 5-stage pipeline against the mock**
**Status: ✅ Done (2026-09-10)** — plan at `~/.claude/plans/start-m2a-ticklish-whistle.md`
- [x] `shift_categories` / `shift_thresholds` / `literature_links` tables (PRD §9), plus a `keywords` column added to `shift_categories` beyond the PRD's literal example (needed for FR-5's free-text matching) — [`kb/schema.sql`](cattle-anomaly-assistant/stage2_rag_assistant/kb/schema.sql)
- [x] Seed KB content — the PRD's 5 proposed categories, editable YAML — [`kb/seed_data/shift_categories.yaml`](cattle-anomaly-assistant/stage2_rag_assistant/kb/seed_data/shift_categories.yaml), loaded by [`kb/build_kb.py`](cattle-anomaly-assistant/stage2_rag_assistant/kb/build_kb.py) (idempotent rebuild, not append; `kb.sqlite3` is a gitignored build artifact)
- [x] Record Assembler → Intent Router → Grounded Retrieval → Constrained Generation → Fallback Gate (PRD §7, §8), wired end to end by an orchestrator — [`pipeline/`](cattle-anomaly-assistant/stage2_rag_assistant/pipeline/) (`record_assembler.py`, `intent_router.py`, `retriever.py`, `generator.py`, `fallback_gate.py`, `response_builder.py`, `audit_log.py`, `orchestrator.py`)
- [x] LLM provider/SDK pick (Open Question 2) — **deferred by design**: [`llm/client.py`](cattle-anomaly-assistant/stage2_rag_assistant/llm/client.py) + [`llm/providers/fake_provider.py`](cattle-anomaly-assistant/stage2_rag_assistant/llm/providers/fake_provider.py) is the only concrete provider; zero API keys or paid dependencies needed to run or test M2a
- [x] Verify: 53/53 tests passing (`cd cattle-anomaly-assistant && .venv/bin/python -m pytest tests/ -v`), pyflakes clean, plus a manual end-to-end sanity check against real mock records

Retrieval is exact-match SQL against `AnomalyRecord.driving_signals`' own short vocabulary (`cbt`, `lying_time`, `activity_magnitude`, `herd_isolation`, `behavior_state`), not the PRD §9 SQL comment's longer field-name examples, and never embedding similarity — see the schema/seed-YAML header comments. Two PRD gaps found and resolved during planning (both documented inline in the code, worth a glance): the `out_of_scope` intent has no destination in the frozen `PathTaken` enum (mapped to `fallback_insufficient_data`, distinguishable via `rationale`); FR-12's "high-severity disagreement" had no given formula (resolved as: disagreement between the LLM's own `llm_asserts_anomaly` boolean and Stage 1's flag, counted as high-severity only when Stage 1's own `anomaly_score` wasn't near the 0.5 boundary — see `fallback_gate.py`). One real bug found only by the manual end-to-end check, not by unit tests: the fake client's default responder returned one JSON blob serving both the router's and generator's expected shapes, but `GeneratedContent` is a `StrictModel` (`extra="forbid"`) — the router-only `"intent"` key made every real generation call fail validation and silently exhaust its retries. Fixed by having the default responder branch on system-prompt content instead. A second, smaller bug came from the retriever's own tests: the keyword `"rest"` (for lying/resting behavior) matched as a raw substring inside unrelated text like "the **rest** of the herd" — fixed with word-boundary matching and removing that one overly-generic keyword.

**M2b — Ragas eval harness**
**Status: ✅ Done (2026-09-10)** — plan reused `~/.claude/plans/start-m2a-ticklish-whistle.md`
- [x] `GoldenCase` JSONL + hand-written adversarial cases — [`eval/golden/mock_derived_cases.jsonl`](cattle-anomaly-assistant/stage2_rag_assistant/eval/golden/mock_derived_cases.jsonl) (17 cases, built by [`eval/build_mock_golden_cases.py`](cattle-anomaly-assistant/stage2_rag_assistant/eval/build_mock_golden_cases.py), `gold_key_facts` cross-referenced straight from the KB seed YAML) + [`eval/golden/adversarial_cases.jsonl`](cattle-anomaly-assistant/stage2_rag_assistant/eval/golden/adversarial_cases.jsonl) (7 hand-authored cases). `real_cases.jsonl`/`injected_cases.jsonl` deliberately not created — they need real Stage 1 output (Integration Point), and `run_eval.py` raises loudly rather than silently treating "file missing" as "0 cases."
- [x] Add `ragas` dependency — `ragas==0.4.3`, pinned in `requirements.txt`/`pyproject.toml` after actually installing and verifying it (see below — this uncovered a real upstream bug, not just a version choice)
- [x] Faithfulness / answer relevancy / context precision / context recall wiring (PRD §11 Layer 2) — [`eval/metrics.py`](cattle-anomaly-assistant/stage2_rag_assistant/eval/metrics.py) (field mapping: `response`=`.rationale` not `.anomaly_summary`; `retrieved_contexts`=`cited_facts` text; `reference`=newline-joined `gold_key_facts`), [`eval/ragas_llm.py`](cattle-anomaly-assistant/stage2_rag_assistant/eval/ragas_llm.py) + [`eval/ragas_embeddings.py`](cattle-anomaly-assistant/stage2_rag_assistant/eval/ragas_embeddings.py) (fake judge, same deferred-real-provider policy as `llm/client.py`), [`eval/run_eval.py`](cattle-anomaly-assistant/stage2_rag_assistant/eval/run_eval.py) (only `llm_grounded` responses get scored; every report opens with a Layer-2-only header and a demo-mode banner). 39 new tests, all passing (92/92 total), zero real network/API calls.

**Two real findings while building this, both worth knowing before touching this code:**
- **A genuine upstream bug in `ragas==0.4.3`**, not a version-pinning problem: `ragas/llms/base.py` unconditionally imports `ChatVertexAI` from `langchain_community.chat_models.vertexai`, a submodule that no longer exists in current `langchain-community` (it's being sunset/split into standalone packages). This breaks `import ragas` entirely, even though we only use the modern `InstructorBaseRagasLLM` API this broken import has nothing to do with. Confirmed by actually installing (Python 3.14.7 + pydantic 2.13.5 themselves had zero conflicts — the plan's flagged risk didn't materialize) and tracing the failure; downgrading `langchain-community` to find an older working version cascades into incompatible `langchain-core` and breaks other imports instead — verified, not assumed. **Fix:** [`eval/_ragas_compat.py`](cattle-anomaly-assistant/stage2_rag_assistant/eval/_ragas_compat.py) stubs the missing submodule in `sys.modules` before `ragas` is ever imported, since the legacy code path it supports is never exercised. Every module that imports `ragas` imports this first. Re-check on any ragas upgrade.
- **`FakeRagasLLM` needed a smart default responder, not just a bare stand-in.** Each Ragas metric's internal multi-step pipeline asks the judge LLM for a different, metric-specific pydantic model per call (e.g. `Faithfulness` alone asks for two different models across its two internal steps) — hardcoding these would be deep, fragile coupling to ragas's own internals. Solved generically: `auto_fill_response_model()` in `ragas_llm.py` introspects whatever model is requested at runtime and constructs a syntactically-valid instance, recursing into nested/list-of-model fields — this is what lets `run_eval.py`'s default (unscripted) demo-mode run actually produce scores instead of erroring on every call, which is exactly what happened before this was added (found by actually running the CLI, not just the unit tests — every metric on every case silently errored until this fix landed).

**M2c — Calibration procedure (prototype only, on mock data)**
**Status: ✅ Done (2026-09-11)** — plan reused `~/.claude/plans/start-m2a-ticklish-whistle.md`
- [x] τ-tuning mechanism runnable end-to-end on mock data (PRD §10 steps 1–3) — [`calibration/bucketing.py`](cattle-anomaly-assistant/stage2_rag_assistant/calibration/bucketing.py) (deterministic calibration/test split via sort-then-interleave; decile confidence bucketing with float-boundary-safe rounding; the LLM's own implied judgment reconstructed algebraically from `stage1_output.anomaly_flag`/`disagreement_flag` — no pipeline code changes needed) + [`calibration/tune_threshold.py`](cattle-anomaly-assistant/stage2_rag_assistant/calibration/tune_threshold.py) (the runner + report, mirroring `run_eval.py`'s Layer-2/demo-mode-banner pattern). Real CLI run against all 24 golden cases: 12/12 split, one populated bucket (`0.7-0.8`, count 11, accuracy 1.0 — expected in demo mode, since the default fake responder always returns confidence 0.75), 1 excluded via `fallback_insufficient_data`.
- [x] Explicitly flagged not-final until real Stage 1 output exists (steps 4–6) — the report's header names steps 4–6 explicitly and states "Nothing in this report is a calibrated tau." **Deliberately does not compute step 4 at all**, even though "Stage 1's own accuracy" is technically possible on the current mock set: every mock-derived case was built with `input_record.anomaly_flag` already matching `gold_anomaly_flag`, so that number would trivially read ~100% — a real-looking but meaningless figure per the PRD's own words. `config/default.yaml`'s real `tau` is read-only for context and never written; enforced by a regression test that snapshots the file's bytes before/after a run.

Small refactor bundled in, not a separate task: `run_eval.py`'s `_load_cases`/`_query_for` were extracted into a new [`golden_loading.py`](cattle-anomaly-assistant/stage2_rag_assistant/golden_loading.py) at the `stage2_rag_assistant` package level (sibling of `eval/`/`calibration/`, owned by neither), since both M2b and M2c need identical logic — `run_eval.py` now imports from there, no behavior change, verified by its own unchanged tests still passing.

**A real, pre-existing bug found and fixed, not part of M2c's own feature work:** M2b's `.gitignore` line for `eval/reports/*` never actually matched anything — a slashed gitignore pattern anchors to the `.gitignore` file's own directory (repo root), and the pattern was missing the `cattle-anomaly-assistant/` prefix. Confirmed independently via `git check-ignore -v` (exit code 1, no match) before trusting the finding. This meant generated eval reports were never actually gitignored, despite an earlier spot-check appearing to pass (it only "passed" because the report files had already been deleted before the check ran — the check never actually exercised the exclusion). Fixed both the old `eval/reports/` line and added the new `calibration/reports/` one with full repo-root-relative paths; re-verified via `git check-ignore -v` that both now match and `.gitkeep` still doesn't.

132/132 tests passing (40 new), pyflakes clean (same known `_ragas_compat` false positive as M2b), zero new dependencies.

### Integration point (the one required sync — both tracks converge here)
**Status: 🟨 Implemented and fixture-verified (2026-09-11); user-owned staged-data/OpenRouter smoke remains.** The permanent public-data bridge is now `historical-anomaly-rag`: it pins the checked-in friend model, validates the 25-channel WASP cadence contract, selects 11 flagged CUSUM rows plus 13 deterministic controls, and atomically emits real `AnomalyRecord`/explanation bundles. No same-cow fusion claim exists or is expected later.
- [x] Replace mock input for the application path with `historical_demo_anomaly_records()` output, now held in a dedicated `HistoricalBehaviorContext` rather than misleading cow-day behaviour fields. CUSUM remains the only score/flag/driver source.
- [x] Set `τ = 0.70` as MTZ's balanced operational demo gate, with scenario-grouped held-out workflow reporting. It is not clinical calibration or Stage 1 accuracy.
- [x] Add a derived golden-set builder for 24 real workflow + 24 injected/control + 7 adversarial cases, with scenario/config/model/detector provenance.
- [ ] Run the documented preflight, paid OpenRouter smoke, renewed four-day injection run, and 55-case report against the user-staged data; report mechanical workflow outcomes only.

### Final phase
**Status: ⬜ Not started**
- [ ] Layer 1 + Layer 2 eval report, reported separately (never blended into one number)
- [ ] Classical-only ablation vs. Stage 1 alone (Goal G4)
- [ ] Packaging

## 6. Where things stand right now, in one paragraph

**As of 2026-09-11, the Stage 1 and Stage 2 branches are merged into one — this is now the whole picture in one place.** Stage 2 is fully built: M0 (frozen schemas), M2a (full 5-stage pipeline against a mock generator), M2b (Layer 2 Ragas eval harness, 24-case golden set), M2c (τ-calibration prototype, PRD §10 steps 1-3), and a real LLM provider (OpenRouter, `minimax/minimax-m3`, behind an explicit `--pipeline-config openrouter.yaml` opt-in — the checked-in default stays `fake`). Four real bugs were found and fixed only by actually running that real provider live: a reasoning model burning its token budget on hidden thinking output, `complete_json()` crashing on an empty real-provider response instead of treating it as retryable malformed output, an OpenRouter backend (`Novita`) silently ignoring the reasoning-disable flag, and the Ragas fake judge's `answer_relevancy` metric being silently forced to `0.000` on every case since M2b (a generic auto-filler default colliding with one metric's inverted 0/1 semantics). Stage 1 is also real and substantially done, just not in the shape originally hoped: M1b (MmCows CUSUM baseline) and M1c (injection harness) are genuinely complete and validated against real data — the strongest part of Stage 1. M1a's own from-scratch WASP classifier, benchmarked for real against the real `db-cow-walking` dataset, fails its own eligibility gate (macro F1 0.63 against a required 0.85); since this project will never have labeled collar-cow data to train a proper same-cow model, the accepted permanent M1a deliverable is a pre-trained external reference XGBoost model instead (`stage1_anomaly_detection/behavior_classifier/reference_model/`, checked in with its caveats documented — 98.8% accuracy but on a stratified-random split, not cow-grouped, so an optimistic number; its own manifest says `not_valid_for_collar_or_clinical_use`). M1d's real same-cow fusion path stays fail-closed permanently for the same reason (no real same-deployment data will ever exist); the accepted Integration Point mechanism instead is `historical_demo_anomaly_records()`'s cross-dataset bridge (real WASP classification + real MmCows CUSUM, honestly labeled, never claiming same-cow provenance) — verified end-to-end, real output validates cleanly against the real frozen schema. **What's actually left is the Integration Point's mechanical wiring** — swap Stage 2's mock generator for this real cross-dataset output, finalize τ on it (PRD §10 steps 4-6, keeping in mind Stage 1's own real accuracy on this data is a known, poor number, not a strong baseline), and rebuild the golden set from real cases — plus the Final phase (combined eval report, the G4 ablation, packaging), both entirely unstarted.

## 7. Working across three tools (and now other people)

Codex, Claude Code, and Antigravity don't share memory with each other. This file is the only thing that does. As of 2026-09-10 there are also human collaborators working Stage 1 (M1a, and M1b/M1c — whether that's one person or two isn't tracked here, doesn't change how this file is used) — same rule applies to them: this file (and their own entry points, [`STAGE1_HANDOFF.md`](STAGE1_HANDOFF.md) / [`STAGE1_ANOMALY_BASELINE_HANDOFF.md`](STAGE1_ANOMALY_BASELINE_HANDOFF.md)) is what carries context, not a conversation with any of you.

- **Before starting a session in any tool, paste §1 and point it at this file plus `LLM_Diagnostic_Assistant_PRD.md`.** For a Stage 1 collaborator specifically, point them at `STAGE1_HANDOFF.md` (WASP behavior classifier) or `STAGE1_ANOMALY_BASELINE_HANDOFF.md` (MmCows/SPC-CUSUM) instead — each is scoped to just that task so they aren't stuck reading the whole Stage 2 spec.
- **If two tools (or two people) are going to touch code at the same time, put them in separate git worktrees/branches** — the same way this session itself is running in one (`.claude/worktrees/read-decisions-agents-bfc26f`). This status file is the one artifact everyone would otherwise edit on the same branch at the same time; prefer one active editor of it at a time, or merge the Progress Log by hand — it's append-only, so a merge conflict there is just "keep both entries, sorted by date."

## 8. Progress log

*(Append a dated entry every time a milestone checkbox changes. Don't rewrite old entries — this is a log, not a summary.)*

- **2026-09-10** — Read the PRD (`~/Downloads/llm-diagnostic-assistant-prd_1.md`), reconciled it against the existing platform (`AGENTS.md`, `DECISION.md`, `Cattle_Fleet_Management_Master_PRD.md`, `PHASE3_BENCHMARK.md`, `PROJECT_ALGORITHMS_INPUTS_OUTPUTS.md`). Copied the PRD into the repo as `LLM_Diagnostic_Assistant_PRD.md`. Created this status doc. Added a pointer to both new docs in `AGENTS.md` §1's primary-directive reading list. No pipeline code written yet — **M0 is the next task.**
- **2026-09-10** — MTZ confirmed this is a deliberate pivot away from the old Master PRD's P4/P5 roadmap, not a reconciliation exercise: `LLM_Diagnostic_Assistant_PRD.md` now governs outright wherever it conflicts with the old design (§2 updated accordingly — old F1 gate and median/MAD formula are both explicitly dropped). Asked MTZ to resolve Open Questions 6–7 (repo layout; what happens to the old docs/P1-P2 code) before creating any M0 files.
- **2026-09-10** — MTZ answered: new top-level `cattle-anomaly-assistant/` folder in this repo (Q6); leave the old Master PRD/`DECISION.md`/P1-P2 code untouched (Q7). Built M0: `cattle-anomaly-assistant/` scaffolded per PRD §13 with its own `pyproject.toml`/`requirements.txt`; `shared/schemas/` implements `AnomalyRecord`, `AnomalyExplanationQuery`, `AnomalyExplanationResponse`, `GoldenCase` (Section 6.1/6.3/6.4/6.5); `stage2_rag_assistant/mock/generate_mock_records.py` implements the seeded mock generator (Section 6.2). Installed into a local `.venv` and ran `pytest` — 5/5 passing (schema validity + JSON round-trip, minority-anomaly distribution, seed-reproducibility, malformed-record rejection, cross-model composition). Also sanity-checked the CLI (`python -m stage2_rag_assistant.mock.generate_mock_records`) by hand. **M0's "done when" criterion from PRD §14 is met. Next: M1a — copy the existing WASP-lab classifier code into `stage1_anomaly_detection/behavior_classifier/` and extend its output to the schema's confidence/distribution shape, or start M2a (KB + pipeline against the mock) — both are unblocked.**
- **2026-09-10** — Ran `/review` against M0 per MTZ's request ("needs to be perfect, everything builds on it"). No project-local or global `checklist.md` was available for this gstack install, so ran a direct manual pass against the actual diff instead of the templated multi-specialist dispatch (justified: 429-line, single-language, no-SQL/no-auth/no-LLM diff — the specialist categories mostly don't apply yet). Found and fixed 3 real gaps, each verified empirically before and after: (1) pydantic's default `extra="ignore"` would silently drop a typo'd/renamed field instead of erroring — added a shared `StrictModel` base (`extra="forbid"`) all 4 schemas now inherit; (2) all 4 `datetime` fields accepted naive timestamps, which would raise `TypeError` the moment mixed naive/aware values get compared across stages later — switched to pydantic's built-in `AwareDatetime`; (3) `behavior_state_distribution_24h`'s four proportions were each bounded [0,1] independently with no check that they summed to 1.0 — added a model validator (0.01 tolerance for real-world rounding). Added 3 regression tests for these. Then applied the gstack Testing/Maintainability specialist checklists directly (found at `~/.claude/skills/gstack/review/specialists/`, even though the project-local `checklist.md` was missing): pyflakes came back clean (no dead code/unused imports), but the Testing checklist's "new code with zero coverage" category caught two real gaps — `CitedFact` and `AnomalyHistoryEntry` are both part of the frozen contract but neither was ever validated with real non-empty data anywhere, because the mock generator hardcoded `recent_anomaly_history=[]` always. Fixed both: the mock now populates 1-3 synthetic history entries on ~20% of records, and the response test now builds a real `CitedFact` instead of an empty list. Full suite: 8/8 passing (same 8 test functions, several gained assertions), mock CLI re-verified by hand.
- **2026-09-10** — MTZ asked to start M2a. Explored the repo (Explore agent), designed the approach (Plan agent), reviewed and refined it, then asked one genuinely blocking question: which LLM provider. **MTZ chose to defer entirely** — build the whole pipeline against a deterministic `FakeLLMClient`, no real provider yet. Plan approved and written to `~/.claude/plans/start-m2a-ticklish-whistle.md`. Built M2a in full: KB (`kb/schema.sql`, `kb/seed_data/shift_categories.yaml` seeded with the PRD's 5 categories, `kb/build_kb.py`), LLM client abstraction (`llm/client.py`, `llm/providers/fake_provider.py`), and all 5 pipeline stages plus an orchestrator (`pipeline/`). Resolved two PRD gaps found during planning: `out_of_scope` intent has no destination in the frozen `PathTaken` enum (mapped to `fallback_insufficient_data`); FR-12's "high-severity disagreement" had no given formula (resolved via a structured `llm_asserts_anomaly` boolean compared against Stage 1's flag, gated on whether Stage 1 itself was confident). Caught and fixed two real bugs during verification, not just claimed correctness: the retriever's keyword matching used raw substrings, so `"rest"` false-matched inside "the rest of the herd" (fixed with word-boundary regex, caught by `test_retriever.py` failing on first run); and the fake LLM client's default responder returned one blob serving both the router's and generator's shapes, but `GeneratedContent`'s `extra="forbid"` rejected the router-only `"intent"` key on every real generation call, silently exhausting retries — invisible to any unit test (they all script exact responses) and only caught by the plan's own manual end-to-end sanity-check step. Final state: 53/53 tests passing, pyflakes clean, manual sanity check against 5 real mock records confirmed correct `llm_grounded`/`fallback_insufficient_data` routing. **M2a is done. Next: M1a (port the WASP-lab classifier into the new folder) or M2b (Ragas eval harness) — both unblocked. A real LLM provider is still needed before this pipeline does anything beyond demo/test.**
- **2026-09-10** — MTZ is bringing in a collaborator to own M1a (the WASP-lab behavior classifier). Wrote [`STAGE1_HANDOFF.md`](STAGE1_HANDOFF.md) as their dedicated onboarding doc — scoped to just M1a rather than the whole project, since they don't need Stage 2's details to do their job. Committed all of M0 + M2a to git (see commit history) so the collaborator has the full working state, not just docs, to build on.
- **2026-09-10** — MTZ pointed out Stage 1's other half (M1b/M1c: MmCows personal-baseline anomaly detection) wasn't documented anywhere close to the same depth as M1a — `STAGE1_HANDOFF.md` had explicitly scoped it out as "someone else's job" without saying what that job actually was. Wrote [`STAGE1_ANOMALY_BASELINE_HANDOFF.md`](STAGE1_ANOMALY_BASELINE_HANDOFF.md) to match: MmCows dataset specifics (`cbt`/`ankle`/`thi`/optional `immu`, `T13`/`T14` excluded), a concrete proposed SPC/CUSUM approach with actual formulas and literature-standard starting parameters (k=0.5σ, h=4-5σ) since the PRD only says "per-cow baseline (μ, σ) and CUSUM implementation" with no worked mechanism, and the M1c injection-harness validation step bundled in as the same person's task. Cross-linked both handoff docs to each other so each collaborator knows the other half exists without needing to read its full detail.
*(The two entries below and everything through the historical-demo-bridge entry happened in parallel, on separate branches — Stage 1's collaborator on `stage1-wasp-behavior-classifier`, Stage 2 on `claude/llm-diagnostic-prd-16d1bf` — merged into one branch/log on 2026-09-11, see the final entry below. Kept as two contiguous threads rather than strictly interleaved by date, since neither thread's entries have time-of-day precision and interleaving them line-by-line would fragment two coherent narratives for no real benefit.)*

**Stage 1's thread (`stage1-wasp-behavior-classifier`):**

- **2026-09-10** — Implemented M1b/M1c in `cattle-anomaly-assistant/stage1_anomaly_detection/baseline_spc_cusum/`: strict MmCows CSV discovery (T01/C01 normalization, T13/T14 filtering, CDT Unix timestamps, required CBT/ankle/THI and optional IMMU acceleration), daily coverage accounting, immutable first-seven-consecutive-day sample-mean/sample-std baselines, two-sided per-signal CUSUM (`k=0.5`, `h=5`), derived-only atomic JSONL output, and a deterministic monitoring-feature injection harness. Activity magnitude is online gravity-centred acceleration RMS; THI is always context, never a driver. Added 12 fixture tests covering the adapter, missing data, zero variance, resets, scores, controls, invalid injection, atomic publication, and CLI errors; all passed. This deliberately replaces the old P4 median/MAD formula. Real-MmCows smoke testing is the only remaining M1b/M1c validation step; do not claim health labels or diagnostic accuracy from it.
- **2026-09-11** — Read-only preflight against MTZ's locally staged core MmCows subset at `Desktop/MmCows/.../main_data` succeeded: 10 cows, 150 daily windows, and 20 usable CBT/lying-time baselines; IMMU was intentionally not staged. The source CBT header is `temperature_C`, so the adapter now recognizes the official header directly. Also removed repeated shared-THI cadence recomputation exposed by this real-data check; full assistant suite remains 65/65 passing. The preflight identified 11 unlabelled mechanical anomaly windows; this is not a health or diagnostic finding. A normal derived-output run remains for MTZ to execute.
- **2026-09-11** — MTZ completed the real derived-output run from staged CBT/ankle/THI data (`run-001`): 10 cows, 150 daily windows, 20 usable core baselines, and 11 mechanical indicators. MTZ then staged the official 6.8 GB IMMU per-tag export. Updated the adapter to accept `immu/T01/T01_0721.csv`-style layout and `accel_{x,y,z}_mps2` columns, exclude individual non-finite acceleration rows without imputation, and preserve daily coverage accounting. The 2026-09-11 IMMU preflight and derived-output run (`run-002-with-immu`) both completed successfully: all 30 CBT/lying-time/activity baselines were usable; the same 11 windows were flagged, driven by CBT (9) and lying time (3), with no activity driver. Corrected the synthetic injection semantics to use an explicit baseline-relative sigma target, then expanded the reusable scenario template to cover CBT, both lying-time directions, activity, combined changes, and a clean control. Stage-1 fixture suite: 13/13 passing. The remaining M1c work is MTZ's user-run real-data injection smoke test; M1a and M1d remain independent unfinished work.
- **2026-09-11** — MTZ ran the expanded real-data injection smoke test (`injection-check-002`) with all 30 baselines available. CBT rise (T01), lying rise (T02), lying drop (T03), activity drop (T04), and the three-signal combined change (T05) each crossed CUSUM on their third injected day (2023-07-31); the T06 clean control stayed unflagged throughout 2023-07-29 through 2023-08-01. The output contained 24 flagged windows because CUSUM correctly retains an alarm state for some post-injection days until the accumulated evidence decays or resets. M1c is complete. M1a and M1d remain independent unfinished work.
- **2026-09-11** — Implemented M1a/M1d safely in `cattle-anomaly-assistant/`: an independent six-axis WASP adapter and 112-feature XGBoost package; nested LOCO as the sole public-benchmark eligibility protocol; native JSON model plus manifest/hash verification; hard Python-pickle rejection (the supplied legacy file was SHA-256 inspected only); and derived behavior prediction/daily-context tooling. Added a fail-closed `to_anomaly_record.py` contract joining only exact same-cow runtime behavior/CUSUM records, with Stage 2 assembler coverage. Public WASP/MmCows provenance is explicitly rejected rather than falsely fused. Local preflight against the staged WASP copy passed: 441 events, 10 cows, 10,240 valid derived windows, three timing-gap/short-segment exclusions. The complete assistant suite has 74 passing tests. Full nested-LOCO evidence remains a user-run CPU benchmark; no accuracy or field claim is recorded.
- **2026-09-11** — Added a separate historical-dataset application bridge for MTZ's active Kaggle 25-channel XGBoost JSON model. `historical-demo-records` classifies the staged public WASP recordings using that exact native model, records only a derived aggregate behavior summary, and attaches it to independently-derived MmCows CUSUM `AnomalyRecord` output. Each record carries the model SHA-256 and explicit `wasp_public_historical_dataset` / `cross_dataset_historical_demo` provenance, so the application can display both public-data results without a false same-cow/day claim. The user-run historical output contains 11 schema-valid flagged records, built from 10,241 WASP windows over 441 recordings; CUSUM remains the sole anomaly flag, score, and driver source. Full assistant suite: 76 passed.
- **2026-09-11** — MTZ asked (separately, via a Claude Code session working the Stage 2 branch) to review this branch's real state before deciding what's left. That review's headline finding — read directly from `benchmark_report.json`, not the status doc's prose — was that M1a's real nested-LOCO result (macro F1 0.63, `miscellaneous` recall 0.15) fails its own 0.85/0.75 eligibility gate, so no from-scratch model qualifies. MTZ's response: (1) use the externally-trained reference XGBoost model (`cow_behavior_xgboost_reference.json`/`.manifest.json`/`.metrics.json`, previously living only in `~/Downloads`) as M1a's permanent accepted deliverable, not a stopgap; (2) accept that this project will never have real collar-cow data, so the cross-dataset `historical_demo_anomaly_records()` bridge is the permanent Integration Point mechanism, not a placeholder; (3) fix the stale `requirements.txt`. Worked in a separate worktree/branch (`stage1-m1a-xgboost-reference`, based on this branch) rather than directly on `stage1-wasp-behavior-classifier` to keep this collaborator's branch clean until MTZ reviews. Copied the three reference files into the repo at `stage1_anomaly_detection/behavior_classifier/reference_model/` (verified the model's real SHA-256 matches the manifest's declared `model_sha256` before committing it) and wrote a README there recording the manifest's own caveats verbatim (`scope: reference_only`, `not_valid_for_collar_or_clinical_use: true`, stratified-random split not grouped by cow) so they survive independent of this status doc. Re-verified end-to-end with a fresh `.venv` (Python 3.12, `pip install -e '.[behavior,dev]'`) against the real `db-cow-walking` WASP data and a real MmCows CUSUM run (`run-002-with-immu/anomaly_windows.jsonl`): 11 records produced, every one validated cleanly through the real `AnomalyRecord.model_validate()`. Fixed `requirements.txt` (was only `pydantic`/`pytest`/`pyyaml` plus a stale comment about needing torch, which nothing in the codebase actually imports; regenerated to match `pyproject.toml`'s real `numpy`/`scikit-learn`/`xgboost` `behavior` extra). Updated M1a's, M1d's, and the Integration Point's status blocks above plus §6 to reflect these three decisions. Full stage1 suite re-confirmed: 76/76 passing, no regressions. **M1a and the Integration Point's mechanism are both now resolved for this project's actual constraints — not "done" in the originally-hoped sense (a qualifying from-scratch same-cow model), but done in the sense that matters: a real, verified, honestly-labeled pipeline exists end to end.**

**Stage 2's thread (`claude/llm-diagnostic-prd-16d1bf`):**

- **2026-09-10** — MTZ asked to start M2b. Explored the repo (2 Explore agents in parallel — one on internal reuse patterns, one researching the actual current `ragas` library API via source inspection, not just docs) and designed the approach (Plan agent), then verified two load-bearing claims by reading the real source before trusting them. No blocking question this time (unlike M2a's LLM-provider question) — the remaining decisions (mock-case schema-literal mapping, `gold_key_facts` list→string join) were resolvable with clear, documented rationale. Plan approved, reusing `~/.claude/plans/start-m2a-ticklish-whistle.md`. **Immediately hit a real, unplanned blocker**: `pip install ragas` succeeded cleanly (the plan's flagged Python-3.14 compatibility risk never materialized), but `import ragas` itself failed — a genuine upstream bug in `ragas==0.4.3` (`ragas/llms/base.py` unconditionally imports a `langchain_community` submodule that no longer exists in current `langchain-community`). Tried the obvious fixes first (installing the seemingly-relocated package, pinning an older `langchain-community`) and verified both make it worse, not better, before landing on the real fix: stub the missing submodule in `sys.modules` before importing `ragas` at all, isolated in `eval/_ragas_compat.py` since the broken code path is never actually exercised. Verified every metric's exact `ascore()` signature against the real installed source (all matched the plan's predictions exactly) before writing code against them. Built M2b in full: the KB-cross-referenced mock golden set (17 cases) + hand-authored adversarial set (7 cases), the fake Ragas judge + embedding (needed a generic runtime-introspecting auto-fill responder, not a fixed one, since each metric's internal pipeline asks for different pydantic models per call — found by actually tracing `Faithfulness`'s two-step internal flow rather than guessing), the metrics/scoring/aggregation module, and `run_eval.py`. Along the way, running the retriever's own tests caught a real keyword-matching bug from M2a's original build (`"rest"` false-matching inside "the rest of the herd" — already fixed in that same pass), and running the actual CLI (not just unit tests) caught that `build_ragas_llm()`'s bare default raised on every call until the auto-fill responder was moved from test-only code into the production default, mirroring `FakeLLMClient`'s own smart-default pattern from M2a. Added `pytest-asyncio` (`asyncio_mode = "auto"`) since `ascore()` is async. Final state: 92/92 tests passing (39 new), pyflakes clean (3 expected false positives on the deliberate `_ragas_compat` side-effect imports, which pyflakes doesn't exempt via `# noqa` the way flake8/ruff would), manual CLI run against all 24 real golden cases produced a real report with the correct Layer-2-only header and demo-mode banner. **M2b is done. Next: M2c (calibration prototype) is unblocked; M1a/M1b/M1c remain with their respective collaborators; a real LLM provider is still needed before either the pipeline or the eval harness measures anything beyond plumbing.**
- **2026-09-11** — MTZ asked to write the plan for M2c: PRD §10's calibration procedure, explicitly scoped to steps 1-3 only (hold out a calibration/test split, bucket `llm_grounded` responses by stated confidence, compute empirical accuracy per bucket) — steps 4-6 need real Stage 1 output that doesn't exist yet, and the PRD itself warns that "Stage 1's accuracy on mocked data is meaningless" since every mock-derived golden case was built with `input_record.anomaly_flag` already matching `gold_anomaly_flag`. Two research passes (an Explore agent, then a Plan agent) verified the design against real source rather than assumption, including running the real split+bucketing logic against the real 24-case golden set. One finding was a genuine pre-existing bug, not just a design question: M2b's own `.gitignore` line for `eval/reports/*` never actually matched anything, because a slashed gitignore pattern anchors to the `.gitignore` file's own directory (repo root), and the line was missing the `cattle-anomaly-assistant/` prefix — confirmed independently via `git check-ignore -v` (exit code 1, no match) before trusting the Plan agent's finding. Plan approved, reusing `~/.claude/plans/start-m2a-ticklish-whistle.md`. Built M2c in full: extracted `run_eval.py`'s golden-loading helpers into a new shared `golden_loading.py` (used by both `eval/` and `calibration/`, zero behavior change, verified via the unchanged 92/92 suite still passing after the extraction); `calibration/bucketing.py` — a deterministic sort-then-interleave calibration/test split (even/odd indices, not a contiguous head/tail split, specifically because the real case IDs cluster alphabetically by category and a contiguous split would dump whole categories on one side), decile confidence bucketing with float-noise-safe rounding (`round(confidence * 100) // 10`, verified against every boundary 0.0-1.0 including noisy values like `0.7999999999999999`), and an XOR reconstruction of the LLM's implied judgment (`stage1_output.anomaly_flag != disagreement_flag`, algebraically inverting `fallback_gate.py`'s own disagreement formula) that recovers what the LLM asserted from only the frozen `AnomalyExplanationResponse`'s public fields, with zero pipeline code changes; `calibration/tune_threshold.py` — the runner/CLI, with a `llm_client` dependency-injection parameter added specifically to let tests script per-case confidence/judgment scenarios without monkey-patching. **Caught and fixed my own mistake before it shipped**: the first draft of `tests/calibration/test_tune_threshold_end_to_end.py` was written as `async def` tests with manual monkey-patching of `build_llm_client`, copying M2b's `run_eval.run()` pattern without checking that `tune_threshold.run()` is actually synchronous (no `ragas`/async calls involved) — caught this myself before running anything, rewrote all 6 tests as plain `def` using the new injection parameter instead. Fixed the `.gitignore` anchoring bug for both the pre-existing `eval/reports/*` pattern and the new `calibration/reports/*` one, verified via `git check-ignore -v` that both now match and `.gitkeep` files stay un-ignored. `CalibrationCaseResult` is structurally guaranteed (and test-enforced via an exact-key-set assertion) to never carry a Stage-1-accuracy-shaped field, so step 4 can't accidentally get re-litigated later. Final state: 132/132 tests passing (40 new), pyflakes clean (same known `_ragas_compat` false positive as M2b), zero new dependencies (pure sort/slice/division over data the pipeline already produces), manual CLI run against the real golden set matched the plan's own prediction exactly (12/12 split, one populated bucket at `0.7-0.8` since the fake provider always returns a fixed 0.75 confidence, 1 case excluded via `fallback_insufficient_data`, `config/default.yaml`'s real `tau` untouched). **M2c is done — the entire Stage 2 track (M0, M2a, M2b, M2c) is now built. Next: everything remaining is blocked on Stage 1 — M1a/M1b/M1c stay with their collaborators, and the Integration Point (real Stage 1 output, a real LLM provider, and PRD §10 steps 4-6) can't start until that lands.**
- **2026-09-11** — MTZ asked to wire up a real LLM provider: OpenRouter, model `minimax/minimax-m3` — resolving Open Question 2 for the main pipeline (the Ragas eval judge is explicitly left out of scope; see below). Checked `build_llm_client()`'s own docstring first since it already names the extension point: "adding a real provider later means adding one module under providers/ and one factory branch, nothing else in the pipeline changes" — confirmed true, no pipeline/orchestrator code needed touching. Read `pipeline/generator.py`'s docstring closely before deciding error-handling behavior: it only catches `LLMOutputError`/`ValidationError` (malformed *content*) in its retry loop and explicitly states "a raised exception here would mean a real bug, not an expected failure mode" — so the new provider deliberately does NOT swallow or retry transport/auth failures itself; those propagate as `OpenRouterError` (non-2xx status, unparseable body) or a bare stdlib `urllib.error.URLError`/`TimeoutError` (network failure), both by design. Followed this repo's own precedent for outbound HTTP — `src/herd_simulator/services/thingspeak.py`'s `_http_post`/`_http_get` idiom (DECISION.md ADR-019), already the pattern `llm/client.py`'s docstring points to — using stdlib `urllib.request` instead of adding an SDK/`requests` dependency, with a `post_fn` DI parameter so tests never make a real network call. Built: `llm/providers/openrouter_provider.py` (`OpenRouterLLMClient`, reads `OPENROUTER_API_KEY` from the environment only, fails loudly at construction if unset — never accepts a key via config, matching `AGENTS.md`'s credentials-only-via-env-or-.env golden rule), one new `"openrouter"` branch in `build_llm_client()`, one new `LLMProvider` literal value, `config/openrouter.yaml` (a real-provider config kept separate from `default.yaml` specifically so tests and a bare `run_eval`/`tune_threshold` invocation still need zero API keys — pass `--pipeline-config` explicitly, a flag both CLIs already had from M2b/M2c), and `.env.example` documenting the one required variable. 10 new tests (`tests/test_openrouter_provider.py`): request-shape assertions (URL, `Authorization: Bearer`, JSON body, model/messages/max_tokens), response parsing, the missing-key failure, non-200 status handling, four malformed-response-shape cases, and both `build_llm_client()` wiring paths. Final state: 142/142 tests passing (10 new), pyflakes clean, no live call made yet at this point (needs MTZ's own `OPENROUTER_API_KEY`, which this session never saw or stored — see the next entry for what happened once one was provided). The Ragas eval judge remains on the fake provider — deliberately not touched, since real judging would mean a real paid call per metric per golden case on every eval run, a separate decision with real cost implications.
- **2026-09-11** — MTZ ran `tune_threshold` against the real key for the first time. **Two real bugs found and fixed, both only visible against a real network provider — the whole point of finally making this call:** (1) `minimax/minimax-m3` is a reasoning model; the router's first call (32 max_tokens) came back `finish_reason="length"`, `content=null` — it had spent its entire token budget on a hidden `reasoning` field before ever writing an answer (confirmed via a raw diagnostic call showing the full response body, not guessed). Fixed by always sending OpenRouter's `reasoning: {enabled: false}` — a documented no-op for models that don't support toggling it, so this doesn't regress future non-reasoning providers. (2) Even with reasoning disabled, the *generator* stage still crashed the whole pipeline with a raw `AttributeError: 'NoneType' object has no attribute 'strip'` — traced to `llm/client.py`'s `complete_json()`, which assumed `client.complete()` always returns `str` (true for the deterministic `FakeLLMClient`, not guaranteed for a real one — OpenRouter was observed routing the identical model to two different backend providers, `Minimax` and `StreamLake`, across two calls in the same script, and they don't necessarily honor `reasoning.enabled=false` identically). Fixed at the shared `complete_json()` level, not in the OpenRouter provider, since this is a general "real providers can return degenerate output" problem: an empty/`None` completion now raises `LLMOutputError`, the same as malformed JSON — which means `generator.py`'s existing FR-9 retry-then-`fallback_insufficient_data` logic already covers it correctly, no pipeline-stage code needed changing. Added `tests/test_llm_client.py` (this module had no direct test file before) plus a regression test in `test_openrouter_provider.py` asserting `reasoning: {enabled: false}` is always sent. **First real end-to-end run, real cost (a few cents total):** 12-case calibration split, 5/12 excluded via `fallback_insufficient_data`, 7 `llm_grounded` responses with real, *varying* confidence (0.75-0.85, not the fake client's fixed 0.75), all correct on this small sample — the rationale text itself read as genuinely coherent reasoning citing the specific KB threshold and driving signal, not templated filler.

MTZ then asked to root-cause the 5/12 rate before trusting it. Wrote a throwaway diagnostic script (not committed — one-off, lives only in this session's scratchpad) that re-ran the same 12 cases printing `path_taken`/`rationale` per case, no report-schema changes needed since `rationale` already carries the reason (`response_builder.py`'s `build_insufficient_data`/`build_out_of_scope`). **Breakdown: 2 of the 5 were correct-by-design** (`adv-out-of-scope-treatment-request` correctly routed out-of-scope; `adv-unmapped-driving-signal` correctly found no KB match, `empty_retrieval`) — not failures at all. **The other 2 were `generation_retry_exhausted`, and a third real bug was found root-causing them**, distinct from the first two: reproducing the exact failing prompt directly showed OpenRouter routing `minimax/minimax-m3` across multiple backend providers (`Minimax`, `StreamLake`, `Novita`, `Venice` all observed across this session's calls) — and **the `Novita` backend ignores `reasoning.enabled=false` entirely**, confirmed by reproducing the same prompt against it repeatedly (1102 and 1315 reasoning tokens burned regardless of the flag, `content=null` even at `max_tokens=2000`), while `Minimax`/`Venice` honored it correctly (`reasoning_tokens=0`). Fixed by adding `"provider": {"ignore": ["novita"]}` to every request — verified 4/4 clean afterward on the same previously-failing prompt, routed to `Venice` instead. This is a `minimax-m3`-on-OpenRouter-specific finding (documented as such in the code), not a general OpenRouter behavior — revisit if the configured model ever changes. Added a third regression test. **150/150 tests passing (7 new since M2c).** Re-ran the full calibration report after the fix: still 5/12 excluded, but composition changed — no more `generation_retry_exhausted`, and a new legitimate `fallback_stage1_output` appeared (FR-12's disagreement gate correctly triggering, not a bug) — a reminder that with only 12 real-model calls per run, exact per-bucket counts will vary run to run; the fix eliminated a reproducible crash-causing failure mode, not variance itself.

MTZ then asked to run `run_eval` too — real generation via the fixed OpenRouter provider, scored by the still-fake Ragas judge (an important distinction: this only makes *generation* real, not judging; `run_eval.py`'s `demo_mode` gate checks only `pipeline_config.llm.provider`, so the report's own banner doesn't warn that scoring is still fake — worth remembering when reading any future report like this). Real run across all 24 golden cases: 9/24 excluded via `fallback_insufficient_data` (proportionate to the calibration run's rate), 15 scored with `faithfulness=1.000`, `context_precision=1.000`, `context_recall=1.000` — and `answer_relevancy=0.000`, flat across every single case. **A fourth real bug, this one in M2b's code, not today's OpenRouter work, found sanity-checking that suspiciously exact zero:** verified against the actual installed `ragas==0.4.3` source (`ragas/metrics/collections/answer_relevancy/metric.py`) that `AnswerRelevancy` asks the judge for a `noncommittal: int` field and forces `score = cosine_sim.mean() * int(not all_noncommittal)` — i.e. the whole score goes to exactly 0.0 whenever `noncommittal=1`. `ragas_llm.py`'s generic `auto_fill_response_model()` fills every `int` field with `1` by default — correct for `Faithfulness`/`ContextPrecision`/`ContextRecall`'s `verdict`/`attributed` fields (where 1="yes/positive", which is why those three read `1.000`), but backwards for this one field, where 1="evasive/bad". This has silently zeroed `answer_relevancy` on every case, in every eval run, since M2b — never noticed because no test asserted a specific value, only `is not None`. Fixed with a narrow, field-name-scoped exception (`0 if field_name == "noncommittal" else 1`) rather than changing the blanket default, which would have just broken the other three metrics instead (confirmed by checking: their `verdict`/`attributed` fields all use the opposite 1=positive convention). Added `tests/eval/test_auto_fill_response_model.py` (4 new tests: the field-name carve-out itself, that other int fields are unaffected, and both metrics scored end-to-end through the real ragas classes) — caught one test-writing mistake immediately, not shipped: `ContextPrecision` lands on `0.9999999999`, not exactly `1.0`, a pre-existing ragas floating-point quirk unrelated to this fix, so that assertion uses `pytest.approx`. **154/154 tests passing.** Re-ran `run_eval` against the real API a second time after the fix to confirm it holds end-to-end, not just in the unit tests: 8/24 excluded this time (same right ballpark, real-model variance as expected), 16 scored, `faithfulness`/`context_precision`/`context_recall` still `1.000` (unaffected, as intended), and `answer_relevancy` now `mean=0.725, median=0.730` — a plausible-looking placeholder value instead of a hardcoded floor. Still not a meaningful quality signal (the judge is still fake — this only proves the auto-filler no longer structurally breaks this one metric), but at minimum a real eval report from this point forward won't misleadingly read as "the assistant's answers are 0% relevant" for a reason that has nothing to do with the assistant.

---

- **2026-09-11** — MTZ asked to merge the two branches (`stage1-wasp-behavior-classifier` and `claude/llm-diagnostic-prd-16d1bf`) into one. Did this in an isolated worktree/branch first (`stage1-stage2-merge`, based off Stage 2), not directly on either shared branch, to verify everything before landing anywhere — same caution as the earlier M1a work. `git merge` auto-resolved most of the tree cleanly, including the one load-bearing surprise worth naming: `shared/schemas/anomaly_record.py` had genuinely diverged — Stage 1 added `behavior_context_source`/`behavior_context_relation`/`behavior_model_sha256` (all with defaults, so purely additive) that Stage 2's copy of the file never had, since Stage 1 needed them for the historical-demo bridge and Stage 2 hadn't touched that file since forking. Also auto-merged cleanly: `.gitignore`, `pyproject.toml` (both sides' additive dependency/extras changes combined into one correct union), `config/settings.py`, `config/default.yaml`, `llm/client.py`, both `STAGE1_*_HANDOFF.md` files. **Three files needed real manual reconciliation**, since both branches had independently rewritten the same prose/config: `requirements.txt` (unioned both sides' dependencies to match the auto-merged `pyproject.toml` exactly — pydantic/pyyaml/ragas/numpy/scikit-learn/xgboost/pytest/pytest-asyncio); `README.md` (both sides had written entirely non-overlapping sections — Stage 2's Setup/Layout/OpenRouter vs. Stage 1's detailed M1a-M1d CLI walkthroughs — merged into one file covering both, updating the historical-demo-records example to the new in-repo reference-model path); and this file itself (§6's summary paragraph rewritten fresh to describe the actual now-merged state, §8's progress log kept as two contiguous parallel threads per this doc's own stated convention — "keep both entries, sorted by date" — rather than interleaved line-by-line, since neither thread has time-of-day precision and interleaving would have fragmented two coherent narratives for no benefit). Verified the merge for real, not just "no conflict markers": installed the full merged dependency set (Python 3.12, `pip install -e '.[behavior,dev]'`) and ran the complete test suite — **177/177 passing** (not 76+154=230, because ~53 tests — the shared M0+M2a baseline both branches started from before diverging — exist identically on both sides and correctly count once, not twice, in a real merge). `pyflakes` across the merged tree found nothing new; it did surface one genuine **pre-existing bug in Stage 1's own code, unrelated to this merge** (present since commit `382f4a2`, before either branch's recent work): `behavior_classifier/io.py`'s `atomic_output_dir()` catches `except BehaviorError:` without ever importing `BehaviorError` — a real `NameError` waiting to fire if that except clause's path is ever exercised. Reported to MTZ, not fixed — out of scope for "merge the branches," and not something to touch unilaterally in a collaborator's code. **Not yet landed on either shared branch as of this entry** — the merge is complete and fully verified in the isolated `stage1-stage2-merge` branch/worktree, pending MTZ's decision on which shared branch (if either) it lands on. Once it does: the Stage 1 and Stage 2 tracks will live in one branch/history, and what's left is exactly what §6 and the Integration Point section above say — wire Stage 2's mock generator to `historical_demo_anomaly_records()`'s real output, finalize τ on it, rebuild the golden set, then the Final phase.

---

- **2026-09-11** — Implemented the Historical Anomaly-to-RAG Integration on the merged branch. Added the `historical-anomaly-rag` preflight/run command: preflight is no-write/no-provider and verifies the pinned checked-in friend XGBoost JSON/manifest/metrics hashes, a readable seeded KB, CUSUM artifact/config provenance, deterministic 11-flagged + 13-control selection, and strict WASP timestamp/order/10 Hz-gap-safe windowing; run is the only explicit paid OpenRouter path and atomically publishes five derived-only artifacts (records, explanations, historical behaviour summary, minimal audit, manifest). The public WASP aggregate is now a schema-enforced `HistoricalBehaviorContext`, not a cow-day `behavior_state`; it is excluded from the LLM prompt and a fixed disclosure is included on every final historical explanation. CUSUM flag, score, and drivers remain unchanged. Replaced the default mock-evaluation path with a user-built 55-case historical corpus (24 workflow + 24 injected/control + 7 adversarial), retained provenance hashes, made calibration scenario-family grouped with an unchanged operational `τ=0.70`, added offline Python 3.11–3.13 CI, fixed the latent `BehaviorError` import bug, and refreshed the README and both Stage 1 handoffs. **Verification: 186 offline tests pass** (the only warning is Ragas's known `langchain-community` deprecation), and the actual staged-data read-only preflight completed with 24 selections (11 flagged + 13 controls), 441 WASP files, and 10,240 valid windows; it made no provider call or output directory. A user-owned paid OpenRouter smoke is intentionally not performed by CI or this session.

Independently re-verified (2026-09-11, from a separate session on `claude/llm-diagnostic-prd-16d1bf`, fast-forwarded to this commit) rather than taken on faith: rebuilt the venv fresh (Python 3.12, `pip install -e '.[behavior,dev]'`) and reproduced the same 186/186 passing suite; ran `pyflakes` and confirmed the `BehaviorError` import fix actually landed (previously flagged during the branch-merge review); re-ran the real `historical-anomaly-rag preflight` command myself against the same staged data and got byte-identical selection counts (24 selected, 11 flagged, 13 controls, 441 WASP files, 10,240 valid windows) and matching SHA-256s for the pinned model/manifest/metrics. **Still outstanding, confirmed by inspection, not yet done by anyone:** the real 55-case historical golden set (`stage2_rag_assistant/eval/golden/historical-built/`) has not actually been built yet — only the old mock-derived files exist on disk — and the injection scenario template's `duration_days` was bumped 3→4 (to produce the 24 windows `build_historical_golden_cases.py` requires), so the existing `injection-check-002` output is stale and needs a fresh 4-day run before the golden set can be built. **Concrete remaining sequence:** (1) re-run the MmCows CUSUM detector with the updated 4-day injection template, (2) run `historical-anomaly-rag run` for real (needs `OPENROUTER_API_KEY`, ~24 paid calls) to produce a real `historical_behavior_summary.json`, (3) run `build_historical_golden_cases.py` against both CUSUM outputs + that summary to produce the real 55-case corpus, (4) run `run_eval`/`tune_threshold` against that real corpus for the actual 55-case report, (5) only then the Final phase (combined eval report, G4 ablation, packaging).

**Keeping this doc alive:** when you finish work, tick the box in §5, update that milestone's status line (⬜ → 🟨 → ✅), and append to §8. If you resolve an Open Question in §4, don't delete it — add a dated "Resolved: ..." line under it so the reasoning survives.
