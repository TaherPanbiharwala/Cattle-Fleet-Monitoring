# ThingSpeak API Keys — NEVER commit real values to source control
# Channel 2 (Simulated Fleet) — needed for live mode POST
THINGSPEAK_WRITE_API_KEY=

# Channel 1 (Physical Collar) — needed for Collar-1 sniffing GET
THINGSPEAK_READ_API_KEY=

# Channel 3 (Configuration) — needed for geofence config GET
THINGSPEAK_CONFIG_READ_API_KEY=
